version: '3'
services:
  app:
    build: .
    container_name: pokemon-cafe-bot-1
    working_dir: /home/app
    ports:
      - 4000:4000
    volumes:
      - ".:/home/app"
      - "/home/app/node_modules"
    command: "npm start"
